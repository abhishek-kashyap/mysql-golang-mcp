{
  "connections": {
    "local": {
      "host": "${LOCAL_DB_HOST}",
      "port": 3307,
      "user": "${LOCAL_DB_USER}",
      "password": "${LOCAL_DB_PASSWORD}",
      "database": "${LOCAL_DB_NAME}",
      "read_only": false,
      "max_rows": 1000
    }
  }
}
